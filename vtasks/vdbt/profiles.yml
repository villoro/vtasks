vdbt:
  target: local

  outputs:
    local:
      type: duckdb
      path: "{{ env_var('PATH_DUCKDB') }}"
      schema: ""

    md:
      type: duckdb
      path: md:villoro
      schema: ""

    memory:
      type: duckdb
      path: ":memory:"
      schema: ""
